<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Thème principal -->
    <style name="Theme.PolyHome" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Style des champs Material (email, mot de passe) -->
    <style name="FancyInput" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusTopStart">16dp</item>
        <item name="boxCornerRadiusBottomEnd">16dp</item>
        <item name="boxStrokeColor">@color/vertbleu</item>
        <item name="hintTextColor">@color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Style des boutons Material -->
    <style name="FancyButton" parent="Widget.Material3.Button">
        <item name="cornerRadius">30dp</item>
        <item name="elevation">6dp</item>
        <item name="backgroundTint">@color/vertbleu</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
